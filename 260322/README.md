# 250322

using `perf` to evaluate the power consumption and time elapsed of the different preprocess method , including sequential (seq) , naive (sort by path length) , radix+union , global union , fptree+union , MCP (most common prefix). 

Output all of the random forest model and reordering path which trained by `ranger`for reproducible.


