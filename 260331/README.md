# 260331
To evaluate the power consumption of the ACAM cells , I wrote a `code.r` to 
1. get global feature interval ( assending order )
2. get min and max feature value of every feature in a path ( local feature interval )
3. get the every local feature interval corresponding to global feature interval in every paths 
4. and try to simulate the result is match or not and get information of local/global feature interval.

